{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Game Server",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/backend/cmd/server/main.go", // Or the specific path to your main file if not main.go
            "env": {}, // You can set environment variables here if needed
            "args": [], // Command-line arguments for your program
            "showLog": true, // Shows delve's output in the debug console
        }
    ]
}